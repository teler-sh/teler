apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "teler.fullname" . }}-test-connection"
  labels:
    {{- include "teler.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "teler.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
